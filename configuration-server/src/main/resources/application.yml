server:
  port: 8888
  
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/deepakmuthekar/config-properties.git
          skip-ssl-validation: true
   
